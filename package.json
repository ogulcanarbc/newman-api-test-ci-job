{
    "name": "postman-newman-jenkins",
    "version": "1.0.0",
    "description": "Project for storing Postman tests",
    "directories": {
        "test": "tests"
    },
    "scripts":{
        "api-tests-ci-demo": "npm-run-all -s newman run dog-api.json --reporters cli,junit --reporter-junit-export newman.xml newman run dog-api.json --reporters cli,junit --reporter-junit-export newman.xml"
   },
    "author": "oğulcan arabaci",
    "dependencies": {
        "newman": "5.5.1"
        "npm-run-all":"4.1.5"
    }
}
